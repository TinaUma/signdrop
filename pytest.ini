[pytest]
# Backend uses bare imports (`from api... import`, `from services... import`)
# and is run with backend/ as the working root, so put it on sys.path here.
pythonpath = backend
testpaths = backend/tests
addopts = -q
