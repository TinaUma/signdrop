# Python
__pycache__/
*.py[cod]
*.pyo
.coverage
.coverage.*
htmlcov/
.venv/
venv/
env/
dist/
build/
*.egg-info/

# Node
node_modules/
.next/
dist/
.cache/

# Data (личные файлы пользователя — не коммитим)
data/signatures/
data/output/

# PyInstaller sidecar build workpath (scripts/build-exe.sh --workpath build_pyi)
backend/build_pyi/

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/
src-tauri/binaries/

# App icon committed as a brand asset: app-icon.png is the 1024px master,
# src-tauri/icons/* are the platform sizes fanned out from it via `tauri icon`.

# Collected build output (installers) — produced by scripts/build-exe.sh / CI
/release/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/

# Env
.env
.env.local

# TAUSIK — весь локальный рантайм фреймворка (venv, БД, конфиг с абсолютными путями)
.tausik/

# Claude Code / TAUSIK internal
.claude/
.claude-project/
_cache/
AGENTS.md
.mcp.json
release-build.log

# Claude Design exports (source files, не деплоим напрямую)
SignDrop-standalone.html

# VPS deployment reference — содержит SSH-ключи, не коммитить НИКОГДА
CI.md
